;struct test {
    int i, j, k, l
}

/; _alloc (uint bytes) [~void]
    ;asm "mov %rax, %rdi"
    ;asm "mov $0, %r10"
    ;asm "call malloc"
;/

/; _delete (~void ptr)
    ;asm "mov %rax, %rdi"
    ;asm "mov $0, %r10"
    ;asm "call free"
;/

/; main (uint argc, ~~uint8 argv) [int]
    # ;~void ptr = _alloc(10)
    # ;_delete(ptr)
    # ;return 0
;/
