/; generate (utils.File fin, fout)

	fin.open()
	fout.create()

	uint8 buf = fin.read()
	/; loop (fin.at_end == false && fout.at_end == false)
		fout.write(buf)
		buf = fin.read()
	;/

	fin.close()
	fout.close()

;/

